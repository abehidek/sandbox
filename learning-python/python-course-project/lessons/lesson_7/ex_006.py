number = int(input('Type a number to see its double, triple and square root: '))
print('The double of {} equals {}, the triple is {} and the square root is {}'.format(number,
                                                                                      number * 2,
                                                                                      number * 3,
                                                                                      number ** (1/2)))
